apiVersion: v1
kind: Service
metadata:
  name: {{ include "kos.fullname" . }}-node-headless
spec:
  clusterIP: None  # This makes it "Headless"
  selector:
    app: kos-node  # Must match the labels in your StatefulSet
  ports:
    - port: 8080
      targetPort: 8080
      name: http